# The typeface behind every bake in this directory, as a font_tool input: a TTF under
# assets/font_source (path relative to it) or an OS-installed face name.  The build reads
# this when it cooks a recipe here that has no "face" line of its own, and the gui's runtime
# baker reads it for sizes no recipe covers, so both bakes come from this one spelling.

face    CascadiaMono
